
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: client-id
            client-secret: client-secret
          google:
            client-id: client-id
            client-secret: client-secret
